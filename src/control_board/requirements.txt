# control_board — runtime dependencies.
#
# Setup:
#   sudo python3 -m pip install -r requirements.txt

# Core — main_loop.py / polling.py / main.py
redis
PyYAML
pymodbus

# RPi I2C / GPIO bridge — board, busio (env_sensors.py)
adafruit-blinka

# Optional environmental sensors (try/except graceful)
adafruit-circuitpython-hdc302x      # preferred, deterministic I2C
adafruit-circuitpython-dht          # fallback
mpu6050-raspberrypi                 # dg5w only

# Optional — env_sensors._median fallback works without numpy
numpy
