# gadgetini exporter — runtime dependencies.
#
# Setup:
#   sudo python3 -m pip install -r requirements.txt
#
# Note: ADS1256 driver is vendored at
#   /home/gadgetini/High-Precision-AD-DA-Board-Code/RaspberryPI/ADS1256/python3
# It transitively requires RPi.GPIO + spidev (listed below).

# Core — data_crawler*.py / sensor_exporter.py
redis
jsons
rich
prometheus-client
numpy

# RPi I2C / GPIO bridge — board, busio (used by HDC302x/DHT11 init)
adafruit-blinka

# Optional environmental sensors (try/except graceful — missing package is OK)
adafruit-circuitpython-hdc302x      # HDC302x temp/humidity (deterministic I2C 0x44)
adafruit-circuitpython-ahtx0        # AHT20 temp/humidity (Adafruit #4566, I2C 0x38; HDC302x 대체)
adafruit-circuitpython-dht          # DHT11 temp/humidity (fallback)
mpu6050-raspberrypi                 # MPU6050 gyro (dg5w only)

# Vendored ADS1256 driver transitive deps
RPi.GPIO
spidev
